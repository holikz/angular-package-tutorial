import { WshAccordionTitle } from './accordion-title.directive';

describe('AccordionTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new WshAccordionTitle();
    expect(directive).toBeTruthy();
  });
});
